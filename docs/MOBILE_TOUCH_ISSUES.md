# 移动端触摸交互问题记录

## 当前实现方案

### 交互设计
1. **键盘按钮 (⌨)** → 切换虚拟键盘开/关
2. **点击终端** → 弹出虚拟键盘
3. **滑动** → 终端内容滚动（默认浏览器行为）
4. **右键菜单** → 复制/粘贴/全选（仅桌面端）

### 技术限制
- xterm 使用 canvas 渲染，无法使用 iOS 原生文字选择
- 移动端使用 xterm 内置的选择功能（长按选择）

## 已完成的功能

- [x] UTF-8 中文输入支持
- [x] iOS Safari 中文输入法修复 (空格/数字/标点符号无法输入)
- [x] 终端底部间距调整
- [x] Tab 双击重命名
- [x] Tab 拖拽排序
- [x] PC 端右键菜单
- [x] 虚拟按键栏（ESC/TAB/CTRL/ALT/方向键）
- [x] visualViewport 适配虚拟键盘

## iOS Safari 中文输入法修复方案

**问题**: iOS Safari 中文输入法无法输入空格、数字、标点符号

**原因**: xterm.js 的 `onData` 回调在 iOS Safari 中文输入法组合输入时，部分字符（空格、数字、标点）不会触发

**解决方案**:
1. 在 textarea 上监听 `input` 事件作为补充
2. 使用时间戳跟踪已发送的数据，避免重复发送
3. `onData` 发送时记录数据和时间戳
4. `input` 事件发送前检查是否已通过 `onData` 发送
